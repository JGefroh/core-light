import { default as Component } from '@core/component';

export default class TrailZoneComponent extends Component {
    constructor(payload = {}) {
        super();
        this.componentType = 'TrailZoneComponent';
    }
} 