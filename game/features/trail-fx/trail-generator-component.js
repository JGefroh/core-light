import { default as Component } from '@core/component';

export default class TrailGeneratorComponent extends Component {
    constructor(payload = {}) {
        super();
        this.componentType = 'TrailGeneratorComponent';
    }
} 